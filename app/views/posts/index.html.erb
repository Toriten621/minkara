<h1>投稿一覧</h1>

<% @posts.each do |post| %>
  <div>
    <h2><%= link_to post.title, post %></h2>
    <p>作成者: <%= post.user.email %></p>
  </div>
<% end %>

<% if user_signed_in? %>
  <%= link_to '新規投稿', new_post_path %>
<% end %>
