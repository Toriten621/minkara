<h2>グループ一覧</h2>
<%= link_to "新規グループ作成", new_group_path, class: "btn btn-primary" %>

<ul>
  <% @groups.each do |group| %>
    <li>
      <%= link_to group.name, group %>（メンバー数: <%= group.users.count %>）
    </li>
  <% end %>
</ul>
